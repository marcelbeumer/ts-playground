{
  "name": "ts-microservice-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --outfile=build/index.js --bundle --platform=node --external:./node_modules",
    "dev": "npm run build -- --watch",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.6.5",
    "esbuild": "^0.14.54",
    "typescript": "^4.7.4"
  }
}
